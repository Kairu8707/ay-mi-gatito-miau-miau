local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local VirtualUser = game:GetService("VirtualUser")
local TweenService = game:GetService("TweenService")

local player = Players.LocalPlayer
local mouse = player:GetMouse()

-- UI Parent Logic
local targetParent
local success, _ = pcall(function() targetParent = CoreGui end)
if not success or not targetParent then targetParent = player:WaitForChild("PlayerGui") end

local char = player.Character or player.CharacterAdded:Wait()
player.CharacterAdded:Connect(function(c) char = c end)

-------------------------------------------------
-- STATES & GLOBALS
-------------------------------------------------
local flying = false
local noclip = false
local spectateTarget = nil
local attachBack, attachFront, attachLeft, attachRight = false, false, false, false
local flySpeed = 80
local infJump = false
local espEnabled = false
local healthEspEnabled = false 
local rapidHeal = false 
local autoClickerEnabled = false
local clickDelay = 0.1
local tracersEnabled = false
local dangerSenseEnabled = false
local dangerSoundEnabled = false
local dangerDistance = 150 
local hasPlayedAlert = false 

local bv, bg, flyConnection
local espFolder = Instance.new("Folder", targetParent)
espFolder.Name = "RogueESP"
local tracers = {}

-- Alert Sound
local alertSound = Instance.new("Sound", game:GetService("SoundService"))
alertSound.SoundId = "rbxassetid://12222242" 
alertSound.Volume = 0.5

-------------------------------------------------
-- PROFESSIONAL V12 UI DESIGN
-------------------------------------------------
local gui = Instance.new("ScreenGui", targetParent)
gui.Name = "RogueHub_V12"
gui.ResetOnSpawn = false

-- Danger Label
local dangerLabel = Instance.new("TextLabel", gui)
dangerLabel.Size = UDim2.new(0, 450, 0, 50)
dangerLabel.Position = UDim2.new(0.5, -225, 0.05, 0)
dangerLabel.BackgroundTransparency = 1
dangerLabel.Font = Enum.Font.GothamBold
dangerLabel.TextSize = 20
dangerLabel.TextColor3 = Color3.fromRGB(255, 60, 60)
dangerLabel.TextStrokeTransparency = 0.4
dangerLabel.Text = ""
dangerLabel.Visible = false

local mainFrame = Instance.new("Frame", gui)
mainFrame.Size = UDim2.new(0, 500, 0, 350)
mainFrame.Position = UDim2.new(0.5, -250, 0.5, -175)
mainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true

local mainCorner = Instance.new("UICorner", mainFrame)
mainCorner.CornerRadius = UDim.new(0, 10)

-- Header Bar
local header = Instance.new("Frame", mainFrame)
header.Size = UDim2.new(1, 0, 0, 40)
header.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
header.BorderSizePixel = 0

local headerCorner = Instance.new("UICorner", header)
headerCorner.CornerRadius = UDim.new(0, 10)

-- Title
local title = Instance.new("TextLabel", header)
title.Size = UDim2.new(1, -20, 1, 0)
title.Position = UDim2.new(0, 15, 0, 0)
title.Text = "ROGUE UTILITY HUB <font color='#00aaff'>V12</font>"
title.RichText = true
title.Font = Enum.Font.GothamBold
title.TextColor3 = Color3.new(1,1,1)
title.TextSize = 14
title.TextXAlignment = Enum.TextXAlignment.Left
title.BackgroundTransparency = 1

-- Sidebar
local sidebar = Instance.new("Frame", mainFrame)
sidebar.Position = UDim2.new(0, 0, 0, 40)
sidebar.Size = UDim2.new(0, 130, 1, -40)
sidebar.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
sidebar.BorderSizePixel = 0

local sidebarLayout = Instance.new("UIListLayout", sidebar)
sidebarLayout.Padding = UDim.new(0, 2)
sidebarLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

-- Page Container
local pageContainer = Instance.new("Frame", mainFrame)
pageContainer.Position = UDim2.new(0, 140, 0, 50)
pageContainer.Size = UDim2.new(1, -150, 1, -60)
pageContainer.BackgroundTransparency = 1

local pages = {}
local currentTab = nil

local function createPage(name)
    local pg = Instance.new("ScrollingFrame", pageContainer)
    pg.Size = UDim2.new(1, 0, 1, 0)
    pg.BackgroundTransparency = 1
    pg.Visible = false
    pg.ScrollBarThickness = 0
    pg.CanvasSize = UDim2.new(0,0,0,0)
    pg.AutomaticCanvasSize = Enum.AutomaticSize.Y
    
    local layout = Instance.new("UIListLayout", pg)
    layout.Padding = UDim.new(0, 6)
    pages[name] = pg
    
    local btn = Instance.new("TextButton", sidebar)
    btn.Size = UDim2.new(1, 0, 0, 35)
    btn.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
    btn.BorderSizePixel = 0
    btn.Text = name
    btn.Font = Enum.Font.GothamMedium
    btn.TextColor3 = Color3.new(0.6, 0.6, 0.6)
    btn.TextSize = 12
    
    btn.MouseButton1Click:Connect(function()
        for _, p in pairs(pages) do p.Visible = false end
        pg.Visible = true
        -- Visual highlight logic could go here
    end)
    return pg
end

-- Create Pages
local combatPage = createPage("Combat")
local movePage = createPage("Movement")
local visualPage = createPage("Visuals")
local miscPage = createPage("Misc")
local specPage = createPage("Spectate")
pages["Combat"].Visible = true

-------------------------------------------------
-- COMPONENT CREATORS
-------------------------------------------------
local function makeButton(parent, text, callback)
    local b = Instance.new("TextButton", parent)
    b.Size = UDim2.new(1, -10, 0, 32)
    b.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
    b.Text = text
    b.TextColor3 = Color3.new(0.9, 0.9, 0.9)
    b.Font = Enum.Font.Gotham
    b.TextSize = 11
    b.BorderSizePixel = 0
    
    local bc = Instance.new("UICorner", b)
    bc.CornerRadius = UDim.new(0, 4)
    
    local stroke = Instance.new("UIStroke", b)
    stroke.Color = Color3.fromRGB(40, 40, 40)
    stroke.Thickness = 1
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

    b.MouseButton1Click:Connect(function()
        callback()
        -- Quick flash effect
        b.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
        task.wait(0.1)
        b.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
    end)
    return b
end

local function makeLabel(parent, text)
    local l = Instance.new("TextLabel", parent)
    l.Size = UDim2.new(1, 0, 0, 20)
    l.BackgroundTransparency = 1
    l.Text = "  " .. text:upper()
    l.TextColor3 = Color3.fromRGB(100, 100, 100)
    l.Font = Enum.Font.GothamBold
    l.TextSize = 9
    l.TextXAlignment = Enum.TextXAlignment.Left
    return l
end

-------------------------------------------------
-- FEATURE LOGIC (PRESERVED)
-------------------------------------------------
local function startFly()
    local root = char:FindFirstChild("HumanoidRootPart")
    if not root then return end
    bv = Instance.new("BodyVelocity", root)
    bv.MaxForce = Vector3.new(9e9, 9e9, 9e9)
    bg = Instance.new("BodyGyro", root)
    bg.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
    bg.P = 10000
    flyConnection = RunService.RenderStepped:Connect(function()
        if not flying then return end
        local cam = workspace.CurrentCamera
        local dir = Vector3.zero
        if UIS:IsKeyDown(Enum.KeyCode.W) then dir += cam.CFrame.LookVector end
        if UIS:IsKeyDown(Enum.KeyCode.S) then dir -= cam.CFrame.LookVector end
        if UIS:IsKeyDown(Enum.KeyCode.A) then dir -= cam.CFrame.RightVector end
        if UIS:IsKeyDown(Enum.KeyCode.D) then dir += cam.CFrame.RightVector end
        if dir.Magnitude > 0 then dir = dir.Unit end
        bv.Velocity = dir * flySpeed
        bg.CFrame = cam.CFrame
    end)
end

local function stopFly()
    if flyConnection then flyConnection:Disconnect() end
    if bv then bv:Destroy() end
    if bg then bg:Destroy() end
end

local function toggleFly()
    flying = not flying
    if flying then startFly() else stopFly() end
end

local function updateESP()
    espFolder:ClearAllChildren()
    if not espEnabled then return end
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= player and plr.Character then
            local highlight = Instance.new("Highlight", espFolder)
            highlight.Adornee = plr.Character
            highlight.FillColor = Color3.fromRGB(255, 0, 0)
        end
    end
end

-------------------------------------------------
-- POPULATING TABS
-------------------------------------------------

-- COMBAT
makeLabel(combatPage, "Auto-Clicker Settings")
local acBtn = makeButton(combatPage, "Toggle Auto-Clicker", function()
    autoClickerEnabled = not autoClickerEnabled
end)
makeButton(combatPage, "Increase Speed (-0.05s)", function() clickDelay = math.max(0.01, clickDelay - 0.05) end)
makeButton(combatPage, "Decrease Speed (+0.05s)", function() clickDelay = clickDelay + 0.05 end)

-- MOVEMENT
makeLabel(movePage, "Essential Movement")
makeButton(movePage, "Toggle Fly (Y)", toggleFly)
makeButton(movePage, "Toggle Noclip (N)", function() noclip = not noclip end)
makeButton(movePage, "Toggle Inf Jump (G)", function() infJump = not infJump end)
makeButton(movePage, "WalkSpeed +10", function() char.Humanoid.WalkSpeed += 10 end)
makeButton(movePage, "Reset Movement", function() char.Humanoid.WalkSpeed = 16 char.Humanoid.JumpPower = 50 end)

-- VISUALS
makeLabel(visualPage, "Enhanced Awareness")
makeButton(visualPage, "Toggle Highlight ESP", function() espEnabled = not espEnabled updateESP() end)
makeButton(visualPage, "Toggle Health Text", function() healthEspEnabled = not healthEspEnabled end)
makeButton(visualPage, "Toggle Snaplines", function() tracersEnabled = not tracersEnabled end)
makeButton(visualPage, "Toggle Danger Sense", function() dangerSenseEnabled = not dangerSenseEnabled end)

-- MISC
makeLabel(miscPage, "Character Utilities")
makeButton(miscPage, "Instant Heal (H)", function() char.Humanoid.Health = char.Humanoid.MaxHealth end)
makeButton(miscPage, "Toggle Rapid Heal", function() rapidHeal = not rapidHeal end)
makeButton(miscPage, "Click TP (T)", function() char.HumanoidRootPart.CFrame = CFrame.new(mouse.Hit.Position + Vector3.new(0,5,0)) end)

-- SPECTATE
local searchBox = Instance.new("TextBox", specPage)
searchBox.Size = UDim2.new(1, -10, 0, 30)
searchBox.PlaceholderText = "Search Player..."
searchBox.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
searchBox.TextColor3 = Color3.new(1,1,1)
searchBox.Font = Enum.Font.Gotham
searchBox.TextSize = 11
Instance.new("UICorner", searchBox).CornerRadius = UDim.new(0, 4)

local specScroll = Instance.new("ScrollingFrame", specPage)
specScroll.Size = UDim2.new(1, 0, 1, -40)
specScroll.BackgroundTransparency = 1
specScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
Instance.new("UIListLayout", specScroll).Padding = UDim.new(0, 4)

local function refreshSpec()
    for _, v in pairs(specScroll:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= player then
            local b = makeButton(specScroll, plr.Name, function()
                spectateTarget = plr
                workspace.CurrentCamera.CameraSubject = plr.Character.Humanoid
            end)
            b.Size = UDim2.new(1, -10, 0, 25)
        end
    end
end
refreshSpec()
makeButton(specPage, "Stop Spectating", function() spectateTarget = nil workspace.CurrentCamera.CameraSubject = char.Humanoid end)

-------------------------------------------------
-- BACKGROUND LOOPS
-------------------------------------------------
task.spawn(function()
    while true do
        if autoClickerEnabled then
            VirtualUser:CaptureController()
            VirtualUser:ClickButton1(Vector2.new(0,0))
        end
        task.wait(clickDelay)
    end
end)

RunService.RenderStepped:Connect(function()
    if noclip and char then
        for _, v in pairs(char:GetDescendants()) do if v:IsA("BasePart") then v.CanCollide = false end end
    end
    
    -- Danger Sense
    if dangerSenseEnabled and char:FindFirstChild("HumanoidRootPart") then
        local closestDist = dangerDistance
        local closestPlayer = nil
        for _, plr in pairs(Players:GetPlayers()) do
            if plr ~= player and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                local dist = (char.HumanoidRootPart.Position - plr.Character.HumanoidRootPart.Position).Magnitude
                if dist < closestDist then closestDist = dist closestPlayer = plr end
            end
        end
        if closestPlayer then
            dangerLabel.Text = "⚠️ DANGER: " .. closestPlayer.Name .. " [" .. math.floor(closestDist) .. " STUDS]"
            dangerLabel.Visible = true
            if dangerSoundEnabled and not hasPlayedAlert then alertSound:Play() hasPlayedAlert = true end
        else
            dangerLabel.Visible = false
            hasPlayedAlert = false
        end
    end

    -- Tracers
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= player and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
            if tracersEnabled then
                if not tracers[plr.Name] then tracers[plr.Name] = Drawing.new("Line") tracers[plr.Name].Thickness = 1 tracers[plr.Name].Color = Color3.new(0, 0.6, 1) end
                local vector, onScreen = workspace.CurrentCamera:WorldToViewportPoint(plr.Character.HumanoidRootPart.Position)
                if onScreen then
                    tracers[plr.Name].From = Vector2.new(workspace.CurrentCamera.ViewportSize.X/2, workspace.CurrentCamera.ViewportSize.Y)
                    tracers[plr.Name].To = Vector2.new(vector.X, vector.Y)
                    tracers[plr.Name].Visible = true
                else tracers[plr.Name].Visible = false end
            elseif tracers[plr.Name] then tracers[plr.Name].Visible = false end
        end
    end
end)

UIS.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode == Enum.KeyCode.RightShift then gui.Enabled = not gui.Enabled end
    if input.KeyCode == Enum.KeyCode.Y then toggleFly() end
    if input.KeyCode == Enum.KeyCode.N then noclip = not noclip end
    if input.KeyCode == Enum.KeyCode.G then infJump = not infJump end
end)
